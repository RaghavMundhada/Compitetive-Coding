#include <bits/stdc++.h>

using namespace std;

#define pb push_back
#define ll long long int
#define mp make_pair
#define rep(i,n) for(i=0;i<n;i++)
#define pii pair<int,int>
vector<ll> a(300);
vector<ll> b(300);

int main()
{
	int t,n,m,i,j,k,e;

	cin>>t;

	while(t--)
	{
		cin>>n>>m>>e;

		rep(i,n)
			cin>>a[i];

		rep(i,m)
			cin>>b[i];


	}

	return 0;
}